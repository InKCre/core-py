# Core dependencies for Cloudflare Workers deployment
# Generated from pyproject.toml

# NOTE: Some dependencies may not be compatible with Cloudflare Workers
# due to native extensions or unsupported libraries.
# Test thoroughly and adjust as needed.

pydantic>=2.11.7,<3.0.0
fastapi>=0.116.1,<0.117.0
sqlalchemy>=2.0.41,<3.0.0
aiohttp>=3.12.14,<4.0.0

# Database drivers - psycopg2-binary may need to be replaced
# with psycopg (pure Python) for Workers compatibility
psycopg2-binary>=2.9.10,<3.0.0
# Alternative: psycopg>=3.1.0  # Pure Python driver

sqlmodel>=0.0.24,<0.0.25
pgvector>=0.4.1,<0.5.0

# HTTP and utilities
requests>=2.32.4,<3.0.0
html2text>=2025.4.15

# AI/ML libraries
openai>=1.97.1,<2.0.0
litellm>=1.75.9

# Data processing
numpy>=2.3.2,<3.0.0
datadot>=1.0.0,<2.0.0

# Tencent Cloud SDK (optional - may not be needed in Workers)
tencentcloud-sdk-python-lke>=3.0.1427,<4.0.0

# NOTE: The following are excluded from Workers deployment:
# - uvicorn (not needed, Workers provide the runtime)
# - apscheduler (not supported, use Cloudflare Cron Triggers instead)
