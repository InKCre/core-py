Extension Host 数据库操作与配置／状态持久化接口改为异步，Host 合同推进到 0.2；旧同步 Extension wheel 必须先停用并升级至匹配的正式版本。
删除运行时 SessionLocal、同步 BlockManager／RelationManager、可选 raw session 持久化及旧 Storage 写入接口；组合写入使用必需的 UoW，独立业务入口需要 await。
