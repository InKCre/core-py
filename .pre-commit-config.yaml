repos:
  - repo: local
    hooks:
      - id: export-requirements
        name: Export requirements.txt
        entry: cmd /c pdm export -o requirements.txt --prod && git add requirements.txt
        language: system
        files: ^pyproject\.toml$
        pass_filenames: false
