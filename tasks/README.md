# Tasks

This directory is the volatility buffer of the repository.

Use it for:

- exploration
- migration notes
- feature plans
- temporary reasoning
- backlog items that are not yet stable enough for durable docs

Do not treat files here as permanent truth. Promote only stable, reusable, expensive-to-rediscover knowledge into `docs/`.
