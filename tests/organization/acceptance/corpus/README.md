# Organization acceptance corpus

These authored fixtures describe two interwoven information worlds for Human-reviewed,
best-effort Organization acceptance. Each file is ordinary source information. The manifest
contains provenance and pre-existing graph facts only; it deliberately contains no expected
Organization behavior, target pair, source set, selected fragment, or score.

The fixtures are repository-authored test material. They do not need separate digests or
external retrieval metadata.
