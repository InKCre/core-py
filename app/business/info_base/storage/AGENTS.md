# Coding guide of `app/business/storage`

There's built-in storages and their id is all negative to avoid conflict with user customized storages.

Here's id convention of built-in storages:

- `-1`: http_image
- `-2`: http_video
- `-3`: http_html